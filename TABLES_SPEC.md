# 心理戦ゲーム 確率テーブル仕様

対象ファイル

- `characters.json` — キャラ定義（嘘率の重み＋真偽×疑わしさの結合確率）
- `dialogue_janken.json` — 予告じゃんけんの台詞テーブル

配置先は `app/src/main/assets/`。ロジックに数値を埋め込まないこと。

---

## 1. 嘘率の算出

```
z = logit(base_lie_rate) + Σ ( weight_i × x_i )
lie_rate = sigmoid(z)
```

`logit(p) = ln(p / (1 - p))`、`sigmoid(z) = 1 / (1 + e^-z)`。
線形加算＋クランプではなくロジット空間で足すことで、重みを強くしても 0〜1 を必ず保つ。

### 状況変数 x_i（すべて 0.0〜1.0 に正規化）

| 変数 | 定義 |
|---|---|
| `stakes` | このゲームの配点 ÷ セッション内最大配点 |
| `elimination_risk` | 脱落ラインとの距離を反転して正規化 |
| `score_gap` | 首位との得点差 ÷ 想定満点 |
| `endgame` | 消化済みゲーム数 ÷ 総ゲーム数 |

### lie_timing と重みの対応

- `RANDOM` — 全重みを小さく。状況にほぼ反応しない
- `CORNERED` — `elimination_risk` `endgame` を大きな正値に
- `TRIVIAL` — `stakes` を**負値**に。低リスク時ほど嘘が増える

`lie_timing` は分類ラベルであり、実処理では使わない。分岐せず重みだけで挙動を作る。

---

## 2. 疑わしさの決定

`presentation` は **P(疑わしさ | 真偽)** の条件付き分布。

```
presentation.honest = { low, mid, high }   // 各行の合計は 1.0
presentation.lie    = { low, mid, high }
```

`low` / `mid` / `high` はそのまま台詞の断言強度に対応する。

- 真偽 → `lie_rate` で決定
- 疑わしさ → 決まった真偽に応じて上表から抽選

この2段階により「嘘っぽい本当」（honest × high）と「嘘っぽい嘘」（lie × high）が自然に発生する。

### アーキタイプ

| archetype | 特徴 |
|---|---|
| `HONEST` | 本当は low、嘘は high に寄る。読める。基準点として常時投入 |
| `OVERACT` | 本当なのに high を多用。プレイヤーの誤射を誘う |
| `BAIT` | 嘘のとき high。わざと見破らせ、別の場面の嘘を通す |
| `FLAT` | 全セルほぼ均等。単発では読めない代わりに嘘率は低め |

---

## 3. 解決順序（厳守）

```
1. 状況変数を算出
2. lie_rate を算出
3. 真偽を抽選            ← truth フラグはここで確定
4. claim を決定          （本当なら実行手と同じ / 嘘なら別の手）
5. presentation から強度を抽選
6. 台詞を選択            ← truth フラグを渡さない
```

**不変条件：ステップ6は `truth` を参照してはならない。**

台詞プールは `voice_id` × `intent` × `intensity` のみで引く。真偽ごとに別プールを持たせた瞬間、プレイヤーは文面を暗記して読み切れるようになり、心理戦が崩壊する。

---

## 4. 台詞テーブル

```
voices[voice_id][intent][intensity] = [ 台詞候補 ... ]
```

スロットは `{claim}`（グー/チョキ/パー）と `{target}`（対象キャラ名）。
同一ラウンド内で直前に使った文字列は再抽選し、繰り返しから状況が推測されるのを防ぐ。

ゲームを追加するときは `claims` と `intents` を差し替えた同形式の JSON を新規に置く。`voices` のキー構造は共通のまま流用できる。

---

## 5. 調整の指針

- **差は極端に。** アーキタイプ間で `high` の値が 0.2 程度しか違わないと、人間はまず気づかない。0.5 以上離す
- **キャラは固定。** 毎回新キャラを出すと、プレイヤー側の学習が積み上がらず設計の核が機能しない。`roster.fixed` の2人は常に登場させる
- **実機で詰めない。** AI 同士だけを回すヘッドレスループ（1000セッション程度）で、アーキタイプ別の実測嘘率・被看破率・勝率を出してから数値を決める

### 検証すべき出力

```
アーキタイプ別 実測嘘率
アーキタイプ別 高強度宣言時の実嘘率
アーキタイプ別 勝率
```

`HONEST` の勝率が最下位に沈み続ける場合は嘘の利得が過大。`BAIT` が常勝する場合は看破ペナルティが不足している。

---

## 6. モブNPC

モブは独自パラメータを持たず、基本タイプへの参照と係数1つだけで定義する。

```json
{ "id": "mob_a", "base": "sou", "intensity_scale": 0.4 }
```

### 導出式

```
w' = w × s
p' = (1 - s) / 3 + s × p
```

`s` は `intensity_scale`（0.0〜1.0）。`s = 1.0` で本家と同一、`s = 0.0` で完全な一様分布（無傾向）になる。

### 設計意図

`base_lie_rate` を下げるのではなく、**コントラストだけを下げる**こと。嘘率を下げると単に正直な NPC が増え、`HONEST` との区別がつかなくなって役割が重複する。

同傾向の弱い版にしておけば、モブは本家の傾向を確認する練習台として機能する。完全ランダムなモブはプレイヤーの学習を妨げるノイズにしかならないため、避ける。

推奨レンジは `s = 0.3 ～ 0.5`。

---

## 7. 立ち絵の割り当て

見た目とアーキタイプは**意図的にずらす**。

| キャラ | 見た目の第一印象 | 実際 |
|---|---|---|
| レン | 軽そうな青年 | 最も正直 |
| モモ | 人畜無害な笑顔 | 本当でも強く断言し、誤射を誘う |
| ソウ | 穏やかで信用できそう | 安い場面で嘘を撒く |
| ミオ | 無表情で最も怪しい | 嘘率は低い |

第一印象で判断すると全員読み違える配置にすることで、プレイヤーは2周目から顔ではなく行動履歴で判断し始める。設計書§50の「人格メタゲーム」はここから立ち上がる。

モブは逆に、記憶に残らない平凡な見た目にして情報量を落とす。覚えるべき対象を4人に限定するための処置。

### 初期3人の選定

`starter` は レン / モモ / ソウ。ミオ（`FLAT`）は解放枠に置く。

`FLAT` は原理的に単発では読めないため、プレイヤーが読み方を習得する前に登場させると、ただの運要素として体感される。基準点（`HONEST`）と分かりやすい罠（`OVERACT` `BAIT`）を先に覚えさせてから投入する。
