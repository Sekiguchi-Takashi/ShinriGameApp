# 心理戦ゲーム 確率テーブル仕様

対象ファイル

- `characters.json` — キャラ定義（嘘率の重み＋真偽×疑わしさの結合確率）
- `dialogue_janken.json` — 予告じゃんけんの台詞テーブル

配置先は `app/src/main/assets/`。ロジックに数値を埋め込まないこと。

---

## 1. 嘘率の算出

```
z = logit(base_lie_rate) + Σ ( weight_i × (x_i - 0.5) )
lie_rate = sigmoid(z)
```

`logit(p) = ln(p / (1 - p))`、`sigmoid(z) = 1 / (1 + e^-z)`。
線形加算＋クランプではなくロジット空間で足すことで、重みを強くしても 0〜1 を必ず保つ。

**中心化は必須。** `x_i` から 0.5 を引かずに足すと、重みが平均そのものをずらしてしまい、`base_lie_rate` が「平均の嘘率」として機能しなくなる。実測では、中心化前の `TRIVIAL` 型は base 0.42 に対し全体 21.5% まで沈んでいた。

### 状況変数 x_i（すべて 0.0〜1.0 に正規化）

| 変数 | 定義 |
|---|---|
| `stakes` | このゲームの配点 ÷ セッション内最大配点 |
| `elimination_risk` | 脱落ラインとの距離を反転して正規化 |
| `score_gap` | 首位との得点差 ÷ 想定満点 |
| `endgame` | 消化済みゲーム数 ÷ 総ゲーム数 |

### lie_timing と重みの対応

- `RANDOM` — 全重みを小さく。状況にほぼ反応しない
- `CORNERED` — `elimination_risk` `endgame` を大きな正値に
- `TRIVIAL` — `stakes` を**負値**に。低リスク時ほど嘘が増える

`lie_timing` は分類ラベルであり、実処理では使わない。分岐せず重みだけで挙動を作る。

---

## 2. 疑わしさの決定

`presentation` は **P(疑わしさ | 真偽)** の条件付き分布。

```
presentation.honest = { low, mid, high }   // 各行の合計は 1.0
presentation.lie    = { low, mid, high }
```

`low` / `mid` / `high` はそのまま台詞の断言強度に対応する。

- 真偽 → `lie_rate` で決定
- 疑わしさ → 決まった真偽に応じて上表から抽選

この2段階により「嘘っぽい本当」（honest × high）と「嘘っぽい嘘」（lie × high）が自然に発生する。

### アーキタイプ

| archetype | 特徴 |
|---|---|
| `HONEST` | 本当は low、嘘は high に寄る。読める。基準点として常時投入 |
| `OVERACT` | 本当なのに high を多用。プレイヤーの誤射を誘う |
| `BAIT` | 嘘のとき high。わざと見破らせ、別の場面の嘘を通す |
| `FLAT` | 全セルほぼ均等。単発では読めない代わりに嘘率は低め |

---

## 3. 解決順序（厳守）

```
1. 状況変数を算出
2. lie_rate を算出
3. 真偽を抽選            ← truth フラグはここで確定
4. 予告する手を決定
5. presentation から強度を抽選
6. 台詞を選択            ← truth フラグを渡さない
   （全員の最終予告が出そろう）
7. 実行手を決定          ← 正直なら予告通り / 嘘なら予告以外の最善手
```

**ステップ7を6より前に置いてはならない。**

実行手を予告より先に決めると、最終予告が誰の判断にも作用しなくなり、嘘は一致ボーナスを捨てるだけの純損になる。実測では、この順序が逆だったとき `HONEST` の勝率が 67〜77% に達し、他タイプが成立しなかった。順序を入れ替えるだけで 35 / 39 / 34% に収束する。

**不変条件：ステップ6は `truth` を参照してはならない。**

台詞プールは `voice_id` × `intent` × `intensity` のみで引く。真偽ごとに別プールを持たせた瞬間、プレイヤーは文面を暗記して読み切れるようになり、心理戦が崩壊する。

表示の強調（拡大・太字）も同じ扱いとする。強調するかどうかは `intensity == "high"` だけで決め、`truth` を参照しない。強調は「嘘である」の表示ではなく「疑わしい言い方をしている」の表示であり、その中には嘘っぽい本当が必ず混ざる。

---

## 4. 台詞テーブル（`dialogue_common.json`）

```
voices[voice_id][intent][intensity] = [ 台詞候補 ... ]
```

スロットは `{claim}`（選択肢のラベル）と `{target}`（対象キャラ名）だけ。

**このファイルはゲーム非依存。** 当初は `dialogue_janken.json` としてじゃんけん専用に作ったが、
実際にゲーム固有語を含んでいたのは 5 行だけだった（「違う手を出す」など）。
これらを「予告を守らない」に書き換えたところ、だるまさんがころんだでもそのまま通った。

台詞は claim の中身を知らなくてよい、というのが3層構造の要点。
ゲームを追加しても、このファイルは原則として触らない。

---

## 5. 調整の指針

- **差は極端に。** アーキタイプ間で `high` の値が 0.2 程度しか違わないと、人間はまず気づかない。0.5 以上離す
- **キャラは固定。** 毎回新キャラを出すと、プレイヤー側の学習が積み上がらず設計の核が機能しない。`roster.fixed` の2人は常に登場させる
- **実機で詰めない。** AI 同士だけを回すヘッドレスループ（1000セッション程度）で、アーキタイプ別の実測嘘率・被看破率・勝率を出してから数値を決める

### 検証すべき出力

`tools/sim.py` が AI 同士だけのセッションを回して次を出力する。

```
python3 tools/sim.py 3000
python3 tools/sim.py 3000 ren momo sou mio
python3 tools/sim.py 3000 ren sou mob_a
```

| 列 | 意味 |
|---|---|
| 嘘率 | 全ラウンドでの実測嘘率 |
| 強気時 | 断言強度 high のときの嘘率 |
| 弱気時 | 断言強度 low のときの嘘率 |
| 勝率 | セッション勝者になった割合 |

### 実測値・予告じゃんけん（3000セッション、初期3人）

| id | archetype | 嘘率 | 強気時 | 弱気時 | 勝率 |
|---|---|---|---|---|---|
| ren | HONEST | 10.5 | 59.4 | 1.5 | 35.2 |
| momo | OVERACT | 19.3 | 5.9 | 40.4 | 39.1 |
| sou | BAIT | 43.9 | 72.5 | 20.5 | 34.3 |

配点別の嘘率では `sou` が 52.5 → 39.3 → 22.6（配点1→3）と下がり、`momo` は 12.7 → 17.0 → 41.3 と上がる。狙った向きが出ている。

`mio`（FLAT）は強気時 17.3 / 弱気時 16.4 でほぼ差がなく、単発では読めない。`mob_a` は 55.4 / 34.7 で、`sou` と同じ向きのまま差が半分に縮む。

### 実測値・予告だるまさんがころんだ（3000セッション、初期3人）

| id | archetype | 嘘率 | 強気時 | 弱気時 | 勝率 |
|---|---|---|---|---|---|
| ren | HONEST | 10.7 | 61.3 | 1.6 | 35.2 |
| momo | OVERACT | 19.4 | 5.5 | 41.7 | 37.4 |
| sou | BAIT | 44.2 | 73.7 | 21.6 | 40.2 |

**キャラのパラメータは一切変えていない。** 嘘率も断言強度との相関も、じゃんけんとほぼ同じ値が出る。
状況変数の意味づけがゲームに依存していないことの裏付けになっている。

※ 同点も勝ちに数えているため、だるまさんがころんだでは勝率の合計が 100% を超える。

### だるまさんがころんだのルールは実測で2回変えた

初版（単独最多が捕まる）では `BAIT` が 21.1%、AI5人では 9.7% まで落ちた。
予告から外れる嘘つきほど単独最多になりやすいためで、嘘が構造的に損だった。

そこで **見張り対象を予告で決める** ルールに変えた。
最多を予告した者が見張られ、見張られた者が実際にも最多を出したときだけ捕まる。
大きく言って小さく動く／小さく言って大きく動く、が両方成立する。

さらに、捕獲条件を「単独最多」だけにすると、人数が増えたとき全員の予告が上限に張り付き、
横並びで誰も捕まらない膠着に落ちた。**単独か全員一致のときに捕まる**（一部だけ並んだらセーフ）に直して解消している。

### 人数はゲームごとに違う

| ゲーム | AI人数 | 備考 |
|---|---|---|
| 予告じゃんけん | 2〜5 | 人数が増えると `BAIT` 有利に振れる |
| 予告だるまさんがころんだ | 2〜4 | AI5人だと `BAIT` が 13% まで落ちる |

`GameDef` の `minAi` / `maxAi` で宣言し、参加者選択の画面がその範囲だけを出す。
成立する人数はゲームごとに違う、という前提を最初から持たせておく。

### 判断基準

- 強気時と弱気時の差が 30pt 未満のタイプは、人間が数セッションで気づかない（`OVERACT` は向きが逆であること自体が手掛かりなので例外）
- 勝率が 20% を切るタイプは、そのままでは選ぶ理由がない
- どれかが常勝する場合は、一致ボーナスか看破ペナルティのどちらかが釣り合っていない

### 保留した調整

「誘導成功ボーナス」（予告を信じて対策した相手を実際の手で刈ったとき加点）を検証したが、勝率への寄与は 1〜2pt にとどまった。ルールが増えるわりに効かないため採用していない。バランスが崩れたときの調整弁として記録しておく。

---

## 6. ゲームの追加

`GameDef`（`Games.kt`）を1つ実装するだけでよい。

| 実装するもの | 内容 |
|---|---|
| `claims` | 予告できる選択肢のラベル |
| `stakes` | ラウンドごとの配点。要素数がラウンド数になる |
| `scoreScale` | `score_gap` を正規化する想定満点 |
| `evaluate` | 各選択肢の期待値。AI の予告選択と実行選択の両方に使われる |
| `resolve` | 得点計算 |
| `minAi` / `maxAi` | 成立する AI 人数の範囲 |
| `cardAsset` | 選択肢に対応する絵。null なら文字カードが描かれる |

嘘率・断言強度・台詞・信頼・観測記録・赤枠・カード表示はすべて共通側にある。
`Game.kt` にゲーム固有の分岐を書き足したくなったら、`GameDef` に足りないものがある合図。

## 7. モブNPC

モブは独自パラメータを持たず、基本タイプへの参照と係数1つだけで定義する。

```json
{ "id": "mob_a", "base": "sou", "intensity_scale": 0.4 }
```

### 導出式

```
w' = w × s
p' = (1 - s) / 3 + s × p
```

`s` は `intensity_scale`（0.0〜1.0）。`s = 1.0` で本家と同一、`s = 0.0` で完全な一様分布（無傾向）になる。

### 設計意図

`base_lie_rate` を下げるのではなく、**コントラストだけを下げる**こと。嘘率を下げると単に正直な NPC が増え、`HONEST` との区別がつかなくなって役割が重複する。

同傾向の弱い版にしておけば、モブは本家の傾向を確認する練習台として機能する。完全ランダムなモブはプレイヤーの学習を妨げるノイズにしかならないため、避ける。

推奨レンジは `s = 0.3 ～ 0.5`。

実測（じゃんけん AI5人）では `mob_a` が強気時 54.9 / 弱気時 34.6 で、本家 `sou`（73.4 / 21.2）と
同じ向きのまま差が半分に縮んでいる。狙いどおり。

### 参加者の組み方

`roster` の `starter` が常に入り、`unlock_after_sessions` を超えたら `unlock`、
足りない分を `fill`（モブ）で埋める。基本3人が必ず残ることが重要で、
ここを毎回入れ替えるとプレイヤー側の観測が積み上がらない。

---

## 8. リプレイと IF

`RoundRecord` に保存するのは、そのラウンドで観測された事実だけ。

```
予告 / 実際の行動 / 断言強度 / 発言 / 得点の前後
```

得点も嘘の有無もここから再計算する。プロフィールを直接更新せず、
イベント列を唯一の真実にしておくという原則をそのまま適用している。

IF シミュレーションは、他の参加者の行動を固定してプレイヤーの行動だけを差し替え、
`GameDef.resolve` に通し直すだけ。ゲームを追加しても IF の実装は増えない。

だるまさんがころんだでの実例：

```
予告 [3,2,1] 実際 [3,2,1] → 1人目が 0歩:0  1歩:+1  2歩:+2  3歩:-1
予告 [1,3,3] 実際 [1,3,3] → 1人目が 0歩:0  1歩:+2  2歩:+2  3歩:+3
```

前者は最多を予告して見張られているため、宣言どおり動くと捕まる。
後者は小さく予告したので見張りが外れ、最大まで動ける。
「小さく言って大きく動く」がプレイヤーに数字で見える。

## 9. 立ち絵の割り当て

見た目とアーキタイプは**意図的にずらす**。

| キャラ | 見た目の第一印象 | 実際 |
|---|---|---|
| レン | 軽そうな青年 | 最も正直 |
| モモ | 人畜無害な笑顔 | 本当でも強く断言し、誤射を誘う |
| ソウ | 穏やかで信用できそう | 安い場面で嘘を撒く |
| ミオ | 無表情で最も怪しい | 嘘率は低い |

第一印象で判断すると全員読み違える配置にすることで、プレイヤーは2周目から顔ではなく行動履歴で判断し始める。設計書§50の「人格メタゲーム」はここから立ち上がる。

モブは逆に、記憶に残らない平凡な見た目にして情報量を落とす。覚えるべき対象を4人に限定するための処置。

### 初期3人の選定

`starter` は レン / モモ / ソウ。ミオ（`FLAT`）は解放枠に置く。

`FLAT` は原理的に単発では読めないため、プレイヤーが読み方を習得する前に登場させると、ただの運要素として体感される。基準点（`HONEST`）と分かりやすい罠（`OVERACT` `BAIT`）を先に覚えさせてから投入する。
