# ShinriGameApp 引き継ぎ

心理戦ゲームアプリ。予告じゃんけんと予告だるまさんがころんだを実装。
起動時にゲームと参加人数を選ぶ。

参加者は 基本3人（レン/モモ/ソウ）→ ミオ（2セッション後に解放）→ モブ の順で埋まる。
基本3人は必ず残る。ここを毎回入れ替えると観測が積み上がらず、設計の核が機能しない。

## 現状 v1.0

- プレイヤー1人 + AI3人（レン / モモ / ソウ）
- 5ラウンド、配点は 1・1・2・1・3
- 依存ライブラリなし。Compose も AndroidX も未使用（プログラマティック View のみ）
- 場のカード表示あり（`res/drawable-nodpi/card_*.png`）。未公開は裏面、結果フェーズで実際の手を表にする
- 断言強度 high の発言はログ上で 1.18倍・太字。**強調は intensity のみで決まり、真偽フラグは参照しない**
- 各席に顔アイコン（`char_*.png`）。`characters.json` の `portrait` から解決
- 予告フェーズ（P_DECLARE / P_FINAL）は画面全体を細い赤枠 + 赤帯で表示
- 各席に信頼バー（`trustInPlayer`、10目盛）と累積一致率を表示
- `Memory.kt` が SharedPreferences にキャラ別の観測を蓄積（セッションをまたぐ）

## ラウンドの流れ

```
予告（同時）
 ↓
予告公開
 ↓
会話（AI各1発言 + プレイヤー1行動）
 ↓
最終予告（同時）
 ↓
実行：実際に出す手を選択（予告と違ってよい）
 ↓
結果・得点
```

AI も実行手を最終予告の**後**に決める。逆にすると最終予告が誰の判断にも作用せず、
嘘が一致ボーナスを捨てるだけの純損になり、正直型が勝率 70% 超で独走する。

## ゲームの追加

`GameDef`（`Games.kt`）を実装して `Games.all()` に足すだけ。
`claims` / `stakes` / `scoreScale` / `minAi` / `maxAi` / `evaluate` / `resolve` / `cardAsset`。

心理まわり（嘘率・断言強度・台詞・信頼・観測記録）と画面はすべて共通側にある。
`Game.kt` にゲーム固有の分岐を書きたくなったら、`GameDef` に足りないものがある合図。

台詞テーブル `dialogue_common.json` はゲーム非依存。`{claim}` と `{target}` を差し込むだけなので、
ゲームを追加しても原則として触らない。

## 得点

### 予告じゃんけん
- 他3人との総当たり　勝ち +配点 / 負け -配点
- 予告一致ボーナス　+配点

### 予告だるまさんがころんだ
- 進んだ歩数がそのまま加点
- 最多を予告した者が見張られる（同数なら全員）
- 見張られた者が実際にも最多を出し、それが単独か全員一致なら捕まって -1
- 一部だけ並んだ場合はセーフ
- 予告一致ボーナス　+1（捕まったラウンドは付かない）

### 共通
- 追及が当たり　+配点 / 外れ　-配点

嘘は読み合いで有利になるが、一致ボーナスを失う。この綱引きが調整の主軸。

## AIの構造

`Engine.lieRate()` が唯一の嘘判定。ロジット空間で状況変数を加算する。
真偽が確定してから `sampleIntensity()` で断言強度を抽選し、そのあと台詞を引く。

**不変条件：`Dialogue.pick()` に真偽フラグを渡さない。**
真偽ごとに台詞プールを分けた瞬間、プレイヤーは文面を暗記して読み切れるようになり、設計が崩壊する。

数値はすべて `app/src/main/assets/characters.json`。詳細は `TABLES_SPEC.md`。

## リプレイと IF シミュレーション

セッション終了後に全ラウンドを振り返れる。`records`（`RoundRecord`）が元データ。

保存しているのは観測された事実（予告・実際の行動・断言強度・発言・得点の前後）だけで、
得点も嘘の有無もそこから再計算する。だから `GameDef.resolve` を変えれば
リプレイと IF の結果も自動的に追従する。

IF は、他の参加者の行動を当時のまま固定して、プレイヤーの行動だけを差し替える。
`Replay.simulate` が一時的な `Actor` を組んで `def.resolve` に通すだけなので、
ゲームを追加しても IF 側の実装は要らない。

## 2人対戦（同じ Wi-Fi）

`Net.kt` / `Versus.kt` / `VersusActivity.kt`。依存ライブラリは増やしていない。

片方が「待ち受ける」、もう片方が相手の IP を入れて「つなぐ」。
ポートは 47821。行区切りのごく単純な文字列でやり取りする。

```
MODE|mode|rounds     形式（ホストが決める）
PICK|npcId           使う NPC
DECLARE|claim|強度   予告
ACT|claim            実際の手
NEXT|                次の戦いへ
```

**審判役は置いていない。** 予告と手が出そろえば、どちらの端末でも同じ入力から
同じ結果になるので、両者が同じ手順で判定する。結果を送り合う必要がない。

### 2つの遊び方

- **2人で対戦** — 互いに予告して出す。予告と違う手を出してよい
- **NPCを操る** — 互いに NPC を1体選ぶ。予告のあと5秒だけ助言できる（正直に出せ／予告と違う手を出せ）。
  5秒経つと NPC が勝手に出す

助言は命令ではない。`Versus.obeyChance()` が信頼と性格から従う確率を出す。
もともと嘘をつきにくい性格ほど、嘘の助言には乗りにくい。
初期信頼は `Memory.initialTrust()`、つまり1人用で積み上げた通算一致率から始まる。

5戦3勝先取と9戦5勝先取。あいこはノーカウント。

### 未検証

実機2台での通信は未確認。特に見るべきは、片方が先に「次の戦いへ」を押したときの進行と、
助言の5秒がずれないか。同じ Wi-Fi でも AP アイソレーションが有効だとつながらない。

## 観測の対称性

AI もプレイヤーを観測する（`Memory` に `id="player"` で蓄積）。

- `beliefHonesty` は今セッションの観測に過去の記録を混ぜる（過去分は 20 回が上限）
- キャラのプレイヤーへの初期信頼は通算一致率から決まる。守ってきた相手ほど勧めが通る
- 参加者選択と終了画面に「相手から見たあなた」を表示する

実測では、記録を引き継ぐと BAIT の勝率が 34.3% → 40.3%（じゃんけん）に上がり、
HONEST が下がる。正直でい続けると読まれる、という圧力が効いている。

## 未実装

- AIゲームマスター
- セッションをまたぐリプレイの保存（現状は起動中のみ）
- AI 人格そのものの変化（現状は確率テーブル固定）
- 2人対戦の自動発見（現状は IP 手入力）
- 2人対戦の結果を `Memory` に反映すること

## 記録の設計

`Memory` に保存するのは観測された事実（観測数・一致数・高強度時の一致数）のみ。
「嘘つき」のような断定的なラベルは保存しない。表示は必ず観測数と確信度を添える。

キャラ ID をキーにしているため、`starter` を固定している限り累積は積み上がる。
毎回別キャラを出す設計にすると、この仕組みは機能しない。

## バランス検証

`tools/sim.py` が AI 同士だけのセッションを回す。Python3 のみで動き、
`characters.json` を直接読むのでビルド不要。

```
python3 tools/sim.py 3000
python3 tools/sim.py 3000 daruma
python3 tools/sim.py 3000 daruma ren momo sou mio
python3 tools/sim.py 3000 janken ren sou mob_a
```

`Engine.kt` / `Game.kt` のロジックを写しているので、**片方を変えたら必ずもう片方も合わせる**。
3000セッションでの実測値と判断基準は `TABLES_SPEC.md` を参照。

## 次にやること

1. 実機で数セッション遊び、ソウが配点1で嘘をつき配点3で正直になるのが体感できるか確認
2. モモが強く断言したときほど正直、という逆相関に引っかかるか確認
3. 人数を増やしたとき、モブが多すぎて読む相手が散らないか実機で確認する
4. IF で「小さく予告して大きく動く」が有利だと分かるか確認する（だるまさん）
5. 数セッション遊んでから、勧めの通りやすさが変わったと体感できるか確認する
