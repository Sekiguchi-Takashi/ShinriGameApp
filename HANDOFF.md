# ShinriGameApp 引き継ぎ

心理戦ゲームアプリ。予告じゃんけんと予告だるまさんがころんだ。
起動時に ゲーム → 勝ち方 → 人数 の順に選ぶ。

## 現状 v2.0.4

得点制をやめ、勝ち残り方式に組み替えた大きな変更。

- 依存ライブラリなし。Compose も AndroidX も未使用（プログラマティック View のみ）
- 参加者は 透 / 千鶴 / 慎吾 →（2セッション後に）茜 / 敏行 → モブ の順で埋まる
- 場のカード表示あり。予告・宣告・本番の各段階で場の内容が入れ替わる
- 予告と宣告のあいだは画面全体を細い赤枠で囲う
- ボタンは 予告・宣告＝薄いオレンジ、本番＝薄い緑、相手選択＝キャラのイメージカラー
- 表情は標準 / 泣く / 怒る / 不安の4種（`char_*_cry` などを名前で解決）

## ラウンドの流れ

```
予告　→　宣告　→　本番
```

- **予告** — こう出すつもり、という表明。あとで変えてよく、人柄の判定には使わない
- **宣告** — 本番で出すものの宣告。黙ることもできる
- **本番** — 実際に出す

予告から本番まで会話はない。駆け引きは宣告そのものに集約している。

### 人柄

| 条件 | 判定 |
|---|---|
| 宣告どおりに出した | 正直者 |
| 宣告と違うものを出した | 嘘つき |
| 宣告しなかった | 小心者 |

だるまさんがころんだでは、宣告より多く動けば嘘つき、少なければ小心者。
ただし **鬼が宣告より早く振り向いた回は、少なく動いても数えない**（ノーカウント）。

人柄は得点ではない。勝敗とは別に数えて、セッション終了時と通算で見せる。

## 勝ち方（Rules.kt）

| ルール | 内容 | 人数 |
|---|---|---|
| 勝ち残り | 負けると✕。３つで脱落。最後の1人が勝ち | 2〜6 |
| トーナメント | 一対一の勝ち抜き。あいこはその場でやり直し | 4 または 6 |
| みんなで勝負 | 毎回の勝者を数える。勝ちは何人でもよい。全7回戦 | 2〜6 |

`Match` がルールごとの進行を持ち、ゲーム側は1ラウンドの勝敗（`RoundResult`）だけを返す。
勝ち = 1、負け = -1、あいこ = 0。この切り分けにより、
ゲームを足してもルール側は触らなくてよい。

だるまさんがころんだはトーナメントに向かないので `supports()` で外している。

## v2.0 で外したもの

得点制をやめたことで、次は使わなくなった。

- 会話フェーズ、追及・説得、信頼値（`dialogue_common.json` は未使用のまま残置）
- リプレイと IF シミュレーション（`Replay.kt` は削除）
- `tools/sim.py`（得点前提のため現状と合わない。冒頭に注意書きあり）

## 2人対戦（同じ Wi-Fi）

`Net.kt` / `Versus.kt` / `VersusActivity.kt`。依存ライブラリは増やしていない。

片方が「待ち受ける」を押すと、`NsdManager` で `_shinri._tcp.` を広告する。
もう片方の一覧にそのまま出るので、IP の入力はいらない。
出てこないときの逃げ道として、IP の手入力も残してある。

ポートは 47821。行区切りのごく単純な文字列でやり取りする。

```
MODE|mode|rounds     形式（ホストが決める）
PICK|npcId           使う NPC
DECLARE|claim|強度   予告
ACT|claim            実際の手
NEXT|                次の戦いへ
```

**審判役は置いていない。** 予告と手が出そろえば、どちらの端末でも同じ入力から
同じ結果になるので、両者が同じ手順で判定する。結果を送り合う必要がない。

### 2つの遊び方

- **2人で対戦** — 互いに予告して出す。予告と違う手を出してよい
- **NPCを操る** — 互いに NPC を1体選ぶ。予告のあと5秒だけ助言できる（正直に出せ／予告と違う手を出せ）。
  5秒経つと NPC が勝手に出す

助言は命令ではない。`Versus.obeyChance()` が信頼と性格から従う確率を出す。
もともと嘘をつきにくい性格ほど、嘘の助言には乗りにくい。
初期信頼は `Memory.initialTrust()`、つまり1人用で積み上げた通算一致率から始まる。

5戦3勝先取と9戦5勝先取。あいこはノーカウント。

## アイコン

ランチャーアイコンは `res/mipmap-*`。グーのカードを模した意匠で、
5段階の密度に通常版と丸版、アダプティブ用の前景を置いている。

背景色は `values/colors.xml` の `ic_launcher_background`。
差し替えるときは `tools/` に生成スクリプトを置いていないので、
元のカード画像から作り直すことになる。

## 2人対戦の記録

「2人で対戦」で自分が予告して自分で出した回だけ、1人用と同じ `Memory` の
`id="player"` に足す。NPC を操った回は本人の予告ではないので数えない。

## CI（重要）

**`.github/workflows/build.yml` は置かない。** 同梱もしない。
push のたびに走らせると GitHub 側の制限に当たって必ず失敗する。

CI はタグ起動の `release.yml` だけに一本化する。`release.yml` と `ci/` は
カタログ管理システムが API 経由で入れてくるので、こちらからは触らない。

`deploy.sh` は毎回 `rm -f .github/workflows/build.yml` を実行してから push する。
ZIP を展開しても消えずに残ることがあるため、自動で片づける。

## 署名と配布

配布ビルドは `deploy.sh` がタグを打ち、Actions（`.github/workflows/release.yml`）が
`ci/appathy.keystore` で署名して Release を作る。これが自作アプリストアに更新として出る。

`release.yml` と `ci/` はカタログ管理システムが API 経由で直接コミットしてくる。
**削除しないこと。** 追跡から外すのも不可。

`app/shinri.keystore` は debug ビルド専用の固定鍵。ランナーには鍵が残らないため、
これが無いとビルドのたびに別のデバッグ鍵になり、上書き更新ができなくなる。
配布には関わらないので、release 側の署名設定はカタログ側に任せてある。

## deploy.sh

恒久仕様。push → pull --rebase → タグ発行までを1コマンドで終える。

```
bash deploy.sh "コミットメッセージ"
bash deploy.sh "コミットメッセージ" notag
```

第2引数に `notag` を渡すと push だけで止まり、タグを打たない。
ビルドを走らせたくない中間コミットに使う。

### 消したファイルは deploy.sh で消す

ZIP を `unzip -o` で展開しても、ZIP に無いファイルは端末に残る。
つまり**こちらでファイルを削除しても、端末側では消えない**。
残ったまま古いコードがコミットされ、コンパイルが落ちる。

削除したファイルは deploy.sh に `rm -f` を1行足して片づける。
現在入っているのは次の2つ。

- `.github/workflows/build.yml`
- `app/src/main/java/com/appathy/shinrigame/Replay.kt`（v2.0 で廃止）

十分に世代が進んだら消してよい。

### タグの決め方（一度詰まった箇所）

次のタグは **実際のタグ一覧**（`git tag` を `sort -V`）から決める。
以前は「最新リリース」を見ていたが、リリース作成が失敗するとタグだけが残り、
次回も同じ番号を計算して「既に存在する」で弾かれ、永久にタグが打てなくなった。
push は成功しているのに Actions が走らない、という形で表面化する。

タグの発行も API ではなく `git push origin タグ名`。
API だと結果を握りつぶしていて失敗に気づけなかった。


恒久仕様。push とタグ発行までを1コマンドで終える。

```
bash deploy.sh "コミットメッセージ"
```

`git pull --rebase origin main` は必須。カタログ管理システムが上記2ファイルを
API から直接コミットしているので、これが無いと push が rejected になる。

タグは直近リリースの末尾を +1 して自動で決まる（無ければ v1.0.0）。

## ハマりどころ

`var myDeclared` は JVM 上で `setMyDeclared(int)` を生成する。
同名の `fun setMyDeclared(h: Int)` を置くとシグネチャが衝突してコンパイルが通らない。
Kotlin では素直に `v.myDeclared = h` と代入する。

### 未検証

実機2台での通信は未確認。特に見るべきは、片方が先に「次の戦いへ」を押したときの進行と、
助言の5秒がずれないか。同じ Wi-Fi でも AP アイソレーションが有効だとつながらない。

## 未実装

- AIゲームマスター
- セッションをまたぐリプレイの保存（現状は起動中のみ）
- AI 人格そのものの変化（現状は確率テーブル固定）
- 2人対戦の観戦・再戦
- 2人対戦のリプレイ
- AIゲームマスター

## AIの動き

`GameDef.aiPick()` が予告・宣告・本番の3段階を返す。
本番で宣告どおりに出すかどうかは、そのキャラの `base_lie_rate` で決まる。
宣告を破るときは、相手の宣告に勝てるものを選ぶ。

黙る確率は `honest.low`（弱く言いがちな性格ほど黙る）から出している。

## 次にやること

1. 3ルールをそれぞれ通しで遊び、勝負がだれずに決着するか確認する
2. あいこの出方が自然か確認する（本番は互いの宣告しか手掛かりがないので、以前より増えるはず）
3. だるまさんの鬼まわり（宣告カウントと振り向き）が分かりやすいか確認する
4. 人柄の集計が偏らないか。小心者ばかりになるようなら、黙る確率を下げる
5. ルールごとのバランスを見るため、sim.py を新ルールに合わせて書き直す
