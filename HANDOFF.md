# ShinriGameApp 引き継ぎ

心理戦ゲームアプリのパイロット版。予告じゃんけんのみ実装。

## 現状 v0.1

- プレイヤー1人 + AI3人（レン / モモ / ソウ）
- 5ラウンド、配点は 1・1・2・1・3
- 依存ライブラリなし。Compose も AndroidX も未使用（プログラマティック View のみ）

## ラウンドの流れ

```
予告（同時）
 ↓
予告公開
 ↓
会話（AI各1発言 + プレイヤー1行動）
 ↓
最終予告
 ↓
実際に出す手を選択（予告と違ってよい）
 ↓
結果・得点
```

## 得点

- 他3人との総当たり　勝ち +配点 / 負け -配点
- 予告一致ボーナス　+配点
- 追及が当たり　+配点 / 外れ　-配点

嘘は読み合いで有利になるが、一致ボーナスを失う。この綱引きが調整の主軸。

## AIの構造

`Engine.lieRate()` が唯一の嘘判定。ロジット空間で状況変数を加算する。
真偽が確定してから `sampleIntensity()` で断言強度を抽選し、そのあと台詞を引く。

**不変条件：`Dialogue.pick()` に真偽フラグを渡さない。**
真偽ごとに台詞プールを分けた瞬間、プレイヤーは文面を暗記して読み切れるようになり、設計が崩壊する。

数値はすべて `app/src/main/assets/characters.json`。詳細は `TABLES_SPEC.md`。

## 未実装

- 信頼値のUI表示（内部では `trustInPlayer` を保持済み）
- セッションをまたぐ永続化（現状は起動ごとにリセット）
- ミオ（FLAT）の解放
- モブNPC（`characters.json` に定義済み、未投入）
- 予告だるまさんがころんだ
- リプレイ / IFシミュレーション

## 次にやること

1. 実機で5ラウンド遊び、ソウが配点1のラウンドで嘘をつき配点3で正直になるのが体感できるか確認
2. 体感できなければ `situation_weights` の `stakes` をさらに負側へ
3. AI同士だけを回すヘッドレスループを追加して実測値を出す
